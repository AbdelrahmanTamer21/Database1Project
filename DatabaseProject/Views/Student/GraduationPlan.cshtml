@model IEnumerable<DatabaseProject.Models.GraduationPlanSemester>

@{
    ViewBag.Title = "GraduationPlan";
}

<h2>GraduationPlan</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.semester_code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.credit_hours)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.semester_code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.credit_hours)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
